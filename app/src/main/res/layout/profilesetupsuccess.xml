<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E8E8E8"
    android:id="@+id/profilesetupsuccess"
    tools:context=".ProfileSetUpSuccess"
    >

    <!-- Back Arrow -->
    <ImageView
        android:id="@+id/btnBack"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="59dp"
        android:src="@drawable/arrowback"
        android:contentDescription="Back button" />

    <!-- Title -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/btnBack"
        android:layout_marginStart="16dp"
        android:layout_marginTop="56dp"
        android:text="Fill Your Profile"
        android:textColor="#000000"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- Main Content Card -->
    <LinearLayout
        android:id="@+id/contentCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvTitle"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginTop="60dp"
        android:background="@drawable/profilesetupsuccessbg"
        android:orientation="vertical"
        android:gravity="center"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        android:paddingTop="40dp"
        android:paddingBottom="40dp">

        <!-- Success Icon -->
        <ImageView
            android:id="@+id/imgSuccessIcon"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/setupsuccess"
            android:contentDescription="Success"
            android:layout_marginBottom="28dp" />

        <!-- Congratulations Text -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Congratulations!"
            android:textSize="24sp"
            android:textColor="@color/darkgreen"
            android:textStyle="bold"
            android:layout_marginBottom="14dp" />

        <!-- Description Text -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Your account is ready to use. You will be redirected to the Sign in page in a few seconds..."
            android:textSize="14sp"
            android:textColor="#666666"
            android:gravity="center"
            android:lineSpacingExtra="4dp"
            android:layout_marginBottom="28dp" />

        <!-- Loading Spinner -->
        <ImageView
            android:id="@+id/imgLoadingSpinner"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/browse"
            android:contentDescription="Loading" />
    </LinearLayout>


</RelativeLayout>